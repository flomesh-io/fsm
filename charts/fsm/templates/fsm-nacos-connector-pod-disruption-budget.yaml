{{- if .Values.fsm.cloudConnector.nacos.enable }}
{{- if .Values.fsm.injector.enablePodDisruptionBudget }}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: fsm-nacos-connector-pdb
  namespace: {{ include "fsm.namespace" . }}
  labels:
    app: fsm-nacos-connector
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: fsm-nacos-connector
{{- end }}
{{- end }}