{{- if .Values.fsm.cloudConnector.consul.enable }}
{{- if .Values.fsm.injector.autoScale.enable }}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: fsm-consul-connector-hpa
  namespace: {{ include "fsm.namespace" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fsm-consul-connector
  minReplicas: {{.Values.fsm.consulConnector.autoScale.minReplicas}}
  maxReplicas: {{.Values.fsm.consulConnector.autoScale.maxReplicas}}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{.Values.fsm.consulConnector.autoScale.cpu.targetAverageUtilization}}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{.Values.fsm.consulConnector.autoScale.memory.targetAverageUtilization}}
{{- end }}
{{- end }}