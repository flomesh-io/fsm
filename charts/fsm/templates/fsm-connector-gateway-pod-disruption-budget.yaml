{{- if .Values.fsm.cloudConnector.gateway.syncToFgw.enable }}
{{- if .Values.fsm.cloudConnector.enablePodDisruptionBudget }}
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: {{ include "fsmConnector.gateway.name" . }}-pdb
  namespace: {{ include "fsm.namespace" . }}
  labels:
    app: {{ include "fsmConnector.gateway.name" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: {{ include "fsmConnector.gateway.name" . }}
{{- end }}
{{- end }}