{{- if .Values.fsm.cloudConnector.machine.enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fsmConnector.machine.name" . }}
  namespace: {{ include "fsm.namespace" . }}
  labels:
    {{- include "fsm.labels" . | nindent 4 }}
    app: {{ include "fsmConnector.machine.name" . }}
spec:
  ports:
    - name: machine-connector
      port: 9090
  selector:
    app: {{ include "fsmConnector.machine.name" . }}
{{- end }}