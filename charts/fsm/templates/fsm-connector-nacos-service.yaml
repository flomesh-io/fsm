{{- if .Values.fsm.cloudConnector.nacos.enable }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "fsmConnector.nacos.name" . }}
  namespace: {{ include "fsm.namespace" . }}
  labels:
    {{- include "fsm.labels" . | nindent 4 }}
    app: {{ include "fsmConnector.nacos.name" . }}
spec:
  ports:
    - name: nacos-connector
      port: 9090
  selector:
    app: {{ include "fsmConnector.nacos.name" . }}
{{- end }}